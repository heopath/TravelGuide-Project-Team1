# 대기열 부하 테스트 결과 — 1차

실행 2026-08-14 · 기준 커밋 `9da1105` (`develop`)

절차는 `docs/qa/booking-queue-load-test.md`, 인계 배경은 `docs/handover-booking-queue-load-test.md`를 봅니다.

---

## 1. 실행 환경

로컬에서만 돌렸습니다. 운영 인스턴스에는 접속하지 않았습니다.

| 항목 | 값 |
| --- | --- |
| PostgreSQL | 16.14 (로컬 컨테이너, `compose.yaml`) |
| Redis | 7-alpine (로컬 컨테이너) |
| 앱 프로필 | `local`, 포트 8090 |
| k6 | `grafana/k6` 도커 이미지 |
| VU | 30 (`per-vu-iterations`, 1회) |
| 시간대 재고 | 10 (`ticket_time_slot_id=1`) |
| 계정 | `loadtest1` ~ `loadtest30` (계정마다 여행 1개) |

접속 대상은 실행 전에 확인했습니다. `inet_server_addr()`가 비어 있고(컨테이너 내부 소켓) 빌드가 Debian pgdg이므로 운영 RDS가 아닙니다.

---

## 2. 정확성 — 재고 초과 예약

부하보다 먼저 확인했습니다. 부하 도구는 처리량만 알려줄 뿐 "두 사람이 같은 자리를 받았는가"를 검사하지 않기 때문입니다.

```bash
BOOKING_QUEUE_CONCURRENCY_TEST=true ./gradlew test --tests "*BookingQueueConcurrencyTest*"
```

```text
재고 10개에 30명이 동시에 몰려도 예약은 10건을 넘지 않는다
tests=1  skipped=0  failures=0  errors=0
```

조건부 실행 테스트이므로 `skipped=0`을 함께 확인했습니다. 건너뛴 것을 통과로 읽으면 안 됩니다.

---

## 3. 회차별 결과

모든 회차에서 VU 30, 재고 10으로 고정하고 `capacity-per-second`만 바꿨습니다.

| 회차 | capacity | 입장 | 성공 | 소진 | 만료 | 평균 | 중앙(p50) | p95 | 최대 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 2 | 30 | 10 | 20 | 0 | 21.1s | 21.1s | 40.8s | 42.2s |
| 2 | **5** (기본) | 30 | 10 | 20 | 0 | 7.5s | 7.5s | 15.0s | 15.0s |
| 3 | 10 | 30 | 10 | 20 | 0 | 3.0s | 3.0s | 6.0s | 6.0s |
| 4 | 20 | 30 | 10 | 20 | 0 | 1.0s | 0.0s | 3.0s | 3.0s |

회차마다 예약을 지우고 재고를 0으로 되돌린 뒤 Redis의 `all-my-trips:booking-queue:*`를 비웠습니다.

### DB 교차 확인

k6 집계만 믿지 않고 매 회차 DB를 확인했습니다.

```text
예약 10건 / 서로 다른 사용자 10명 / 재고 reserved 10 = total 10
```

**어떤 capacity에서도 재고를 넘겨 팔지 않았고, 한 사용자가 두 자리를 받은 경우도 없었습니다.**

---

## 4. 읽는 법

### capacity를 올리면 대기 시간은 반비례로 줄어듭니다

`capacity`를 2 → 20으로 열 배 올리자 p95가 40.8초 → 3.0초로 줄었습니다. 성공·소진 건수는 어느 회차에서나 동일했습니다. **대기열은 누가 먼저 들어가느냐만 조절하며 몇 명이 살 수 있는지는 재고가 정합니다.**

### 이 수치는 서버 지연이 아니라 사용자 체감입니다

스크립트는 화면과 같은 **3초 주기로 순번을 확인**합니다. 그래서 측정값은 "서버가 입장시킨 시각"이 아니라 "사용자가 자기 차례를 알게 된 시각"입니다. 폴링 주기만큼 올림이 되며, capacity 20 회차의 중앙값 0초와 최대 3초가 그 경계입니다.

값을 고를 때는 이쪽이 맞는 기준입니다. 사용자는 서버 내부 시각이 아니라 화면이 바뀌는 순간을 기다립니다. 다만 **폴링 주기를 줄이면 같은 capacity에서도 체감이 줄어듭니다.** capacity와 폴링 주기는 함께 봐야 합니다.

---

## 5. 권장 값

**기본값 5를 유지합니다.**

- p95 15초는 대기 화면을 보여주는 목적에 맞습니다. 대기열이 실제로 동작하는 것이 보이면서 이탈할 만큼 길지 않습니다
- 10 이상으로 올리면 p95가 6초 아래로 내려가 30명 규모에서는 대기열이 거의 드러나지 않습니다. 기능을 시연하기 어렵습니다
- 2는 실습 규모에서 지나칩니다. p95 40초는 사용자가 새로고침하거나 창을 닫습니다

**다만 이 값은 30 VU · 재고 10 기준입니다.** 실사용 동시 접속이 이보다 크게 늘면 다시 재야 합니다. 지금 근거는 "이 규모에서 대기 화면이 자연스럽게 보이는 선"이지 처리 한계가 아닙니다.

---

## 6. 측정 도구에서 고친 것

준비물은 이번이 첫 실행이었고, 그대로는 측정이 불가능했습니다. 발견 순서대로 적습니다.

| # | 문제 | 영향 |
| --- | --- | --- |
| 1 | `RedisBookingQueueStore`·`BookingQueueService`가 Jackson 2 `ObjectMapper`를 빈으로 주입받음 | **`local`·운영에서 앱이 아예 기동되지 않음.** `@Profile("!ui")` 때문에 `ui`에서만 감춰져 있었음 |
| 2 | k6가 CSRF 토큰 없이 로그인 시도 | 모든 VU가 로그인에서 403으로 중단 |
| 3 | 모든 VU가 같은 `TRIP_ID` 사용 | 예약은 여행 소유자만 가능해 29명이 거부됨. 측정값이 무의미 |
| 4 | `http_req_failed` 임계값이 재고 소진 409를 실패로 집계 | 정상 동작인데 매 회차가 threshold 위반으로 끝남 |
| 5 | `handleSummary`가 `queue_wait_to_ready_ms`를 출력하지 않음 | **대기 시간이 측정되지 않음.** k6 기본 요약까지 대체해 timing이 전부 사라짐 |

1번은 대기열 기능 자체의 결함이므로 부하 테스트와 무관하게 고쳐야 합니다.

3번을 고칠 때 `TRIP_ID`를 지우지 않고 남겨 두었습니다. VU 하나로 디버깅할 때는 특정 여행을 고정하는 편이 낫기 때문이며, 지정하지 않으면 각 VU가 `GET /api/v1/trips`로 자기 여행을 찾습니다.

### 로컬 환경에서 걸린 것

`uuid-ossp`와 `hstore` 확장이 없어 Flyway `V10__vector_store.sql`이 실패했습니다. `docker-entrypoint-initdb.d`는 **데이터 디렉터리가 비어 있을 때만** 실행되므로, 볼륨을 만든 뒤에 추가된 확장은 반영되지 않습니다. 오래된 볼륨을 쓰는 사람은 같은 곳에서 막힙니다.

```bash
docker compose exec -T postgres psql -U allmytrips -d all_my_trips \
  -c 'CREATE EXTENSION IF NOT EXISTS hstore;' \
  -c 'CREATE EXTENSION IF NOT EXISTS "uuid-ossp";'
```

---

## 7. 다음에 볼 것

- **폴링 주기와 함께 재기** — 지금 체감 대기는 3초 주기에 갇혀 있습니다. 주기를 줄였을 때의 체감과 요청량을 함께 봐야 합니다
- **재고를 늘려서 재기** — 재고 10에서는 20명이 곧바로 소진으로 빠집니다. 재고가 크면 대기열이 더 오래 유지되어 다른 곡선이 나옵니다
- **VU를 늘려서 재기** — 30은 실습 규모입니다. capacity 5의 적정성은 규모에 따라 달라집니다
- **만료 경로 확인** — 이번에는 만료가 0건이라 `admission-ttl`·`processing-ttl` 동작을 확인하지 못했습니다 → **2차에서 확인했고 버그가 있었습니다. 아래를 봅니다**

---

# 2차 — 2026-08-14

기준 커밋 `415c71c`([PR #228](https://github.com/heopath/TravelGuide-Project-Team1/pull/228) 머지 후). 환경·VU·재고는 1차와 같습니다.

목적이 둘이었습니다. **1차가 못 본 만료 경로를 확인하는 것**과, **고친 코드에서 1차 수치가 재현되는지 보는 것**입니다.

## 8. 만료 경로 — 버그가 있었습니다

1차 7절의 마지막 항목입니다. 확인해보니 **실제로 동작하지 않는 경로가 있었습니다.**

대기표가 입장(READY)이 되는 길이 두 개인데, 한쪽만 만료 시간이 제대로 걸려 있었습니다.

| 경로 | 적용된 시간 | |
| --- | --- | --- |
| 줄 서고 → 차례가 옴 (`STATUS_SCRIPT`) | `admission-ttl` (2분) | 정상 |
| 줄 없이 → 바로 입장 (`ENQUEUE_SCRIPT`) | `entry-ttl` (10분) | **잘못됨** |

`ENQUEUE_SCRIPT`가 WAITING이든 READY든 가리지 않고 `entry-ttl`로 만료를 걸고 있었습니다. **2분이면 놓아야 할 자리를 10분간 붙잡고 있었던 셈입니다.**

한산할 때는 대부분 바로 입장하는데 그때는 아무도 안 기다리니 티가 안 납니다. **몰릴 때만 손해가 나는** 종류라 그동안 안 보였습니다.

### 확인 방법

`admission-ttl=8s`로 짧게 주고 Redis 키 수명을 직접 봤습니다.

```bash
docker compose exec redis redis-cli TTL "all-my-trips:booking-queue:entry:<토큰>"
```

| | 입장 직후 대기표 수명 | 9초 뒤 |
| --- | --- | --- |
| 수정 전 | **179초** (= `entry-ttl` 3분) | 14초 뒤에도 예약 성공 |
| 수정 후 | **7초** (= `admission-ttl`) | 조회·예약 모두 410 만료 |

승급 경로도 함께 봤고 원래대로 잘 됩니다(TTL 8초 → 만료). 두 경로가 이제 같아졌습니다.

---

## 9. 회차별 결과

| 회차 | capacity | 입장 | 성공 | 소진 | 만료 | 평균 | p95 | **응답 p95** | 실패율 | 체크 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| A | **5** (기본) | 30 | 10 | 20 | 0 | 7.55s | 15.06s | **255ms** | 0.0% | 100% |
| B | 10 | 30 | 10 | 20 | 0 | 3.01s | 6.02s | **895ms** | 0.0% | 100% |

**대기 시간은 1차와 같습니다.** 1차 2·3회차(p95 15.0초 / 6.0초)가 그대로 재현됐습니다. 고친 코드에서도 대기열 동작은 변하지 않았다는 뜻입니다.

DB도 매 회차 확인했습니다. `reserved 10 = total 10`, 예약 10건, 서로 다른 사용자 10명. 서버 ERROR 로그 0건.

### 새로 본 것 — capacity를 올리면 서버가 느려집니다

1차에서 안 잰 축입니다. capacity를 5 → 10으로 올리자

- 기다리는 시간 p95: 15.06초 → 6.02초 (**60% 감소**)
- 개별 응답 시간 p95: 255ms → 895ms (**3.5배 증가**)

한 번에 들여보내는 사람이 많아지면 같은 재고를 두고 DB에서 동시에 다투기 때문입니다. **줄이 짧아진 대신 창구가 느려집니다.**

1차 4절의 "capacity를 올리면 대기 시간은 반비례로 줄어듭니다"에 **공짜는 아니라는 단서**가 붙습니다.

### 권장 값 — 기본값 5 유지 (1차와 같음)

결론은 1차와 같고 근거가 하나 늘었습니다. 5에서 응답 255ms는 안정적이고, 10으로 올리면 대기 화면이 거의 안 보이는 데다 응답까지 느려집니다.

---

## 10. 측정 도구에서 고친 것 (6절에 이어서)

| # | 문제 | 영향 |
| --- | --- | --- |
| 6 | 만료를 응답 본문의 `status`로 판별 | **만료가 구조적으로 집계되지 않음.** 1차의 "만료 0건"은 만료가 없었던 게 아니라 셀 수 없었던 것 |
| 7 | `handleSummary`가 실패율·체크 통과율을 출력하지 않음 | 임계값 통과 여부를 종료 코드로만 알 수 있어 회차 기록에 남길 숫자가 없음 |

6번을 풀어 쓰면, 만료는 **HTTP 410 + `BOOKING_QUEUE_EXPIRED`** 로 옵니다. 서버가 예외를 던지므로 `data`가 아예 없습니다. 그런데 스크립트는 200이 아니면 반복문을 빠져나와서, 만료가 "차례가 오지 않음"으로 잡혔습니다.

5번과 같은 뿌리입니다. **직접 만든 요약이 k6 기본 출력을 통째로 덮기 때문에**, 찍지 않은 값은 존재하지 않는 것과 같습니다.

---

## 11. 다음에 볼 것

1차 7절에서 만료 경로만 지웁니다. 나머지는 그대로입니다.

- **폴링 주기와 함께 재기**
- **재고를 늘려서 재기**
- **VU를 늘려서 재기** — 계정 30개가 상한입니다. `fixtures.sql`의 `generate_series` 범위를 넓혀야 합니다
- **`processing-ttl` 확인** — `admission-ttl`은 봤지만 이건 아직입니다
- **여러 시간대 동시 부하** — 지금 스크립트는 `SLOT_ID` 하나만 씁니다

---

# 3차 — 2026-08-16

2차 11절에 남아 있던 세 가지를 잽니다. 도구를 먼저 열고(`POLL_SECONDS`·`DOUBLE_SUBMIT`·`fixtures` 변수) 네 회차를 돌렸습니다.

**가장 큰 소득은 숫자가 아니라 구조입니다.** 대기열이 시간이 아니라 **조회할 때 전진한다**는 것을 확인했습니다. 이게 1·2차의 수치를 다시 읽게 만듭니다.

## 12. 실행 환경

1차와 같되 계정과 재고를 늘렸습니다.

| 항목 | 값 |
| --- | --- |
| 앱 | `local` 프로필, `bootRun` (개발 모드) |
| k6 | 도커 이미지 `grafana/k6` — **설치하지 않았습니다** |
| 계정 | 30개 (D회차만 60개) |
| 재고 | 10개 (D회차만 25개) |
| `capacity-per-second` | 5 (기본값, 전 회차 고정) |

> **응답 시간의 절대값은 1·2차와 비교하지 마세요.** 이번에는 `bootRun` 개발 모드(JIT 제한)라 2차보다 느립니다. 회차 간 상대 비교만 유효합니다.

## 13. 회차별 결과

| 회차 | 조건 | 입장 | 성공 | 소진 | 만료 | 평균 대기 | p95 대기 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| A | VU 30 · 주기 3초 (기준) | 30 | 10 | 20 | 0 | 7.60초 | **15.17초** |
| B-1 | VU 30 · **주기 1초** | 30 | 10 | 20 | 0 | 2.54초 | **5.07초** |
| B-5 | VU 30 · **주기 5초** | 30 | 10 | 20 | 0 | 12.53초 | **25.07초** |
| C | VU 30 · 주기 3초 · **완료 2회 동시** | 30 | 10 | 19 | 0 | 7.53초 | 15.06초 |
| D | **VU 60 · 재고 25** · 주기 3초 | 60 | 25 | 35 | 0 | 16.58초 | **33.15초** |

전 회차 **요청 실패율 0%, 체크 통과율 100%**입니다.

### DB 교차 확인

모든 회차에서 재고를 넘겨 팔지 않았습니다.

| 회차 | 재고 | `reserved_quantity` | 예약 항목 | 서로 다른 사용자 |
| --- | ---: | ---: | ---: | ---: |
| A | 10 | 10 | 10 | 10 |
| B-1 | 10 | 10 | — | 10 |
| C | 10 | 10 | 10 | 10 |
| D | 25 | 25 | 25 | 25 |

## 14. 대기열은 시간이 아니라 조회할 때 전진합니다

A·B-1·B-5의 p95를 나란히 놓으면 **폴링 주기의 정확히 5배**입니다.

| 주기 | p95 | 주기 대비 |
| ---: | ---: | ---: |
| 1초 | 5.07초 | **×5.07** |
| 3초 | 15.17초 | **×5.06** |
| 5초 | 25.07초 | **×5.01** |

우연이 아닙니다. `RedisBookingQueueStore`의 순번 조회 Lua 스크립트가 **조회하는 김에 대기자를 승급시킵니다.** 별도 스케줄러가 없어서, 아무도 조회하지 않으면 줄은 전혀 줄지 않습니다.

승급 수는 초 단위 카운터(`EXPIRE 2`)로 `capacity-per-second`만큼 제한됩니다. 그런데 30명이 같은 주기로 폴링하면 **한 번의 조회 물결마다 5명씩** 넘어갑니다. 그래서 대기 시간이 이렇게 됩니다.

```text
체감 대기 ≈ (앞에 선 사람 수 ÷ capacity) × 폴링 주기
```

D회차가 이 식을 뒷받침합니다. VU를 60으로 두 배 늘리자 p95가 15.17초 → 33.15초로 **두 배가 됐습니다**(60 ÷ 5 × 3초 = 36초, 실측 33초).

### 그래서 1·2차의 결론이 바뀝니다

1차 4절 "capacity를 올리면 대기 시간은 반비례로 줄어듭니다"는 맞습니다. 다만 **폴링 주기를 줄여도 똑같이 줄어듭니다.** 두 값은 곱으로 붙어 있습니다.

2차에서 capacity를 5 → 10으로 올렸을 때 응답 시간이 255ms → 895ms로 3.5배 느려졌습니다. **폴링 주기를 줄이는 쪽은 그 대가가 없습니다.** DB 경쟁이 늘지 않기 때문입니다.

| 대기를 절반으로 줄이는 방법 | 대가 |
| --- | --- |
| capacity 5 → 10 | 응답 3.5배 느려짐 (DB 경쟁) |
| 폴링 주기 3초 → 1.5초 | 조회 요청 2배 (가볍고 Redis만 씀) |

**폴링 주기 쪽이 먼저입니다.**

> 응답 시간 p95는 주기가 다른 회차끼리 비교할 수 없습니다. 주기를 줄이면 값싼 상태 조회의 비중이 커져 p95가 오히려 내려갑니다(A 1423ms → B-1 336ms). 같은 주기끼리만 비교하세요.

## 15. `processing-ttl` — 잠금이 동작합니다

C회차에서 같은 토큰으로 예약 완료를 **동시에 두 번** 불렀습니다. 사용자가 예약 버튼을 두 번 누르는 상황입니다.

| 결과 | 건수 |
| --- | ---: |
| `BOOKING_QUEUE_PROCESSING` 거부 | **26** |
| 같은 예약 재생 (`completed-ttl`) | 0 |
| 양쪽 모두 재고 소진 | 4 |

**예약 성공은 10건으로 A회차와 같았고, 같은 사용자가 두 건을 예약한 경우는 0건입니다.** 잠금이 실제로 이중 예약을 막았습니다.

재생이 0건인 것은 두 요청이 워낙 촘촘히 붙어 앞 요청이 끝나기 전에 뒤 요청이 도착했기 때문입니다. 즉 `PROCESSING` 경로를 지났다는 뜻이라 오히려 의도한 결과입니다.

## 16. 권장 값

**`capacity-per-second` 기본값 5를 유지합니다.** 1·2차 결론과 같습니다.

바뀐 것은 **체감 대기를 줄이려면 capacity가 아니라 폴링 주기를 먼저 보라**는 점입니다. 지금 화면은 3초 주기입니다. 1.5초로 줄이면 대기가 절반이 되고 서버 부담은 거의 늘지 않습니다.

다만 이번 측정은 모든 사용자가 **같은 주기로 규칙적으로** 폴링하는 상황입니다. 실제로는 창을 닫거나 탭을 백그라운드로 두는 사람이 섞이므로, 줄이 그만큼 천천히 전진할 수 있습니다.

## 17. 다음에 볼 것

2차 11절에서 세 가지가 지워졌습니다. 남은 것과 새로 생긴 것입니다.

- **폴링 주기를 실제 화면에서 줄여보기** — 이번 측정은 k6 기준입니다. `queue.js`의 주기를 바꾸고 재는 것은 별도입니다
- **불규칙한 폴링** — 일부가 조회를 멈추면 줄이 어떻게 되는지. 지금 구조에서는 조회가 곧 전진이라 영향이 있을 수 있습니다
- **여러 시간대 동시 부하** — 지금 스크립트는 `SLOT_ID` 하나만 씁니다
- **운영 환경에서 재기** — 지금까지 전부 로컬이고, 이번 회차는 개발 모드라 응답 시간이 특히 느립니다
