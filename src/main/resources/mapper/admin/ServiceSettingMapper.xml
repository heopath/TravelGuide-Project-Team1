<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.all_my_trip_project.domain.admin.mapper.ServiceSettingMapper">

    <select id="findValue" resultType="string">
        SELECT setting_value
        FROM service_settings
        WHERE setting_key = #{settingKey}
    </select>

    <!-- 최초 행이 유실되어도 관리자 저장 한 번으로 복구할 수 있게 upsert한다. -->
    <insert id="upsert">
        INSERT INTO service_settings (setting_key, setting_value, updated_by)
        VALUES (#{settingKey}, #{settingValue}, #{updatedBy})
        ON CONFLICT (setting_key) DO UPDATE
        SET setting_value = EXCLUDED.setting_value,
            updated_by = EXCLUDED.updated_by
    </insert>

</mapper>
