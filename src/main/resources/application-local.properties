spring.docker.compose.enabled=false
spring.autoconfigure.exclude=\
org.springframework.boot.data.redis.autoconfigure.DataRedisAutoConfiguration,\
org.springframework.boot.data.redis.autoconfigure.DataRedisReactiveAutoConfiguration,\
org.springframework.boot.data.redis.autoconfigure.DataRedisRepositoriesAutoConfiguration,\
org.springframework.ai.model.google.genai.autoconfigure.chat.GoogleGenAiChatAutoConfiguration,\
org.springframework.ai.model.google.genai.autoconfigure.embedding.GoogleGenAiEmbeddingConnectionAutoConfiguration,\
org.springframework.ai.model.google.genai.autoconfigure.embedding.GoogleGenAiTextEmbeddingAutoConfiguration,\
org.springframework.ai.vectorstore.pgvector.autoconfigure.PgVectorStoreAutoConfiguration

# local 프로필 전용이며 Release/운영 DB 인증정보는 환경변수·Secret으로 주입한다.
# 기본값은 DBeaver와 동일한 SSH 터널의 로컬 포트이며 운영/개인 환경에서는 환경변수로 재정의한다.
# 비밀번호는 저장소에 기록하지 않고 ALL_MY_TRIP_DB_PASSWORD 환경변수로만 주입한다.
spring.datasource.url=${ALL_MY_TRIP_DB_URL:jdbc:postgresql://localhost:15432/all_my_trip}
spring.datasource.username=${ALL_MY_TRIP_DB_USERNAME:allmytrip_dev}
spring.datasource.password=${ALL_MY_TRIP_DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

mybatis.mapper-locations=classpath:/mapper/**/*.xml
mybatis.type-aliases-package=org.example.all_my_trip_project.domain
mybatis.configuration.map-underscore-to-camel-case=true

spring.jpa.hibernate.ddl-auto=none
# 기존 팀 RDS는 V1~V7 구조가 수동 적용되어 있어 V7을 기준선으로 기록하고 V8부터 자동 적용한다.
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=7

spring.ai.model.chat=none
spring.ai.model.embedding.text=none
